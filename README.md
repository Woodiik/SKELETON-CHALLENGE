# Skeleton Challenge — Vue + Sails.js blog

Test task. Vue 3 on the frontend, Sails.js + PostgreSQL on the backend, JWT auth, blog with comments.

The first 10 posts on the home page are rendered server-side via EJS. Everything after that is fetched and rendered by a Vue component.

WIP — setup docs and env example will land here once the basics are in.
